language: python
python:
  - "2.7"

install:
  - pip install -r requirements.txt
  - python setup.py develop
  - pip install coveralls

before_script:
  - coverage report -m

script:
  - py.test

after_success:
  - coveralls
