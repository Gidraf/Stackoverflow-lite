language: python
python:
  - "2.7"

install:
  - pip install -r requirements.txt
  - python run.py get_question

before_script:
  - source stackoverflow/.travis.sh

script:
  - cd stackoverflow/tests
  - py.test --cov= test/

after_success:
  - coveralls
